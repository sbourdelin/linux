#
# Cavium ThunderX Soc drivers
#
config CAVIUM_LMC
        depends on ARCH_THUNDER
	def_tristate m

config CAVIUM_OCX
	depends on ARCH_THUNDER
	def_tristate m

config CAVIUM_OCX
        depends on ARCH_THUNDER
	def_tristate m
